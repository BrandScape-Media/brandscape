node_modules
dist
.vscode
.idea
*.local
.env
.env.local
tsconfig.tsbuildinfo
system-prompts/
image-integration/

# Influencer reference images = private generation assets, never in the
# PUBLIC repo. The real library lives in Supabase + R2 (managed via admin).
src/assets/Influencer-library/

# Claude session context - local only, never publish (public repo)
CLAUDE.md

# Business strategy: COGS, margins, target customers, outreach tactics.
# PRICING.md and LAUNCH.md were committed before this rule existed and are
# already public — see STRATEGY.md §9.
STRATEGY.md
