node_modules
dist
.vscode
.idea
*.local
.env
.env.local
tsconfig.tsbuildinfo
system-prompts/
image-integration/

# Influencer reference images = private generation assets, never in the
# PUBLIC repo. The real library lives in Supabase + R2 (managed via admin).
src/assets/Influencer-library/

# Claude session context - local only, never publish (public repo)
CLAUDE.md
