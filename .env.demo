# Demo-mode dev server (`vite --mode demo`): blanking Supabase makes the app
# boot logged-out with fake data — used to preview/verify the demo dashboard.
# No secrets here; safe to commit.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
